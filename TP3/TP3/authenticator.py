class Authenticator:
            def __init__(self, users):
                self.users = []

                def add_user(self, usernme, password):
                    self.username = username
                    self.password = password

                    def login(self, username, password):
                        self.username = username
                    self.password = password

                    def username(self, password):
                        self.password = password

                        def logout(self, username):
                            self.username = username